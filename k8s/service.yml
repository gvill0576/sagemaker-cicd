apiVersion: v1
kind: Service
metadata:
  name: sagemaker-fastapi-svc
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
spec:
  selector:
    app: sagemaker-fastapi
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80